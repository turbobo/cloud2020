server:
  port: 3355

spring:
  application:
    name: config-client
  cloud:
    config:
      label: test #分支
      name: config  #服务名
      profile: test  #环境
      uri: http://localhost:3344  #中心端地址
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"